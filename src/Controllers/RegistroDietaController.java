package Controllers;

import Views.Menus;

public class RegistroDietaController {
    public RegistroDietaController(Menus menu) {
        int opc = 0;
        while (opc != 3) {

            switch (opc) {
                case 1:
                    break;
            }
        }
    }
}
